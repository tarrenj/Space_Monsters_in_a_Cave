// Initialize object stats
enum direction(none = 0, left, down, up, right);
enum collision(no = 0, enemy, loot, wall);
int x_pos, y_pos;
int speed = 0;

// Setters and Getters
void public get_health;
void public get_strength;
void public get_armor;
